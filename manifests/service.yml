apiVersion: v1
kind: Service
metadata:
    name: walletmaterializedviewprocessor
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: walletmaterializedviewprocessor